apiVersion: ml.jay.dev/v1
kind: TrainingJob
metadata:
  name: test-concurrent-4
spec:
  image: busybox
  command: ["sh", "-c"]
  args: ["echo job 4 started; sleep 5; echo job 4 done"]
  maxRetries: 1
