apiVersion: ml.jay.dev/v1
kind: TrainingJob
metadata:
  name: test-concurrent-3
spec:
  image: busybox
  command: ["sh", "-c"]
  args: ["echo job 3 started; sleep 5; echo job 3 done"]
  maxRetries: 1
