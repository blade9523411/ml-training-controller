apiVersion: ml.jay.dev/v1
kind: TrainingJob
metadata:
  name: test-concurrent-5
spec:
  image: busybox
  command: ["sh", "-c"]
  args: ["echo job 5 started; sleep 5; echo job 5 done"]
  maxRetries: 1
