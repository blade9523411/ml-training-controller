apiVersion: ml.jay.dev/v1
kind: TrainingJob
metadata:
  name: test-checkpoint-retry
spec:
  image: trainer:dev
  command: ["python", "/app/train.py"]
  args: ["--epochs", "5", "--fail-at-epoch", "2"]
  maxRetries: 2
  checkpointPath: "/checkpoints"
