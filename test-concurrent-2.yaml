apiVersion: ml.jay.dev/v1
kind: TrainingJob
metadata:
  name: test-concurrent-2
spec:
  image: busybox
  command: ["sh", "-c"]
  args: ["echo job 2 started; sleep 5; echo job 2 done"]
  maxRetries: 1
